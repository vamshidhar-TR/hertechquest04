# Optional. Without a key the app runs fully on deterministic fallbacks:
#   - natural-language rule parsing  -> regex/keyword parser
#   - "why this matters" explanations -> templated, tax-aware text
# Add a key to light up Claude-powered explanations and NL rule parsing.
ANTHROPIC_API_KEY=

# Port the API listens on (frontend proxies /api here).
PORT=3001
